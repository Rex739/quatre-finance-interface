import ActiveLink from "../../activeLink";

const NavList = ({ isClicked, handleClick }) => {
  return (
    <aside className={""}>
      <nav>
        <ul>

        </ul>
      </nav>
    </aside>
  );
};
export default NavList ;

{
  /* <ActiveLink
  href="https://app.quatre.finance"
  target="_blank"
  rel="noreferrer"
  handleClick={handleClick}
>
  <img
    src={lightMode ? "images/navbar/app.svg" : "images/navbar/app-dark.svg"}
    alt="app"
    width={40}
    height={40}
    className="me-2"
  />{" "}
  App
</ActiveLink>; */
}
